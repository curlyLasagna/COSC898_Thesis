

\chapter{Research Questions}\label{RQ}

\begin{enumerate}
	\item Can an AI agent accurately infer system-level dependencies from natural language assignment instructions or unconfigured source code to automatically generate valid flake.nix environments?
	\item{How does the computational footprints (memory overhead, disk deduplication across heterogeneous course suites, and cold-start latency) of Nix Flakes, and OCI Dev Containers compare?}
	\item{While containerization and declarative environments have solved the 'works on my machine' problem in software engineering, what continues to hinder their adoption in university classrooms?}
\end{enumerate}

\chapter{Proposed Solution}\label{chap:solution}




\chapter{Testing}\label{chap:testing}

\section{Agent flake synthesis testing}

Answering RQ1 will allow instructors to

\subsection{MCP-NixOS}

mcp-nixos is a FastMCP server that provides real-time information about the Nix ecosystem. Information could be Home Manager settings,

It leverages search.nix.org Elastic search backend

nixpkgs is a massive repository. At the time of this writing, nixpkgs hosts 139,008 packages across 1,044 packages sets. Not all of the


\cite{ImportingPhantomsMeasuring} observes that Rust has the highest package hallucination rates, which suggest that large language models have a higher chance of hallucinating package names due to the scarcity of Rust code compared to Python and Javascript. Nix isn't a popular language

\subsection{Test Setup}

To automate the tests, we wrote a Python script that invoke system commands

\subsubsection{Antigravity cli}

An agent that is readily available to the public

\subsubsection{Opencode with Ollama}

OpenCode has the ability to communicate with an LSP server. The LSP server used in the generation of our flake file

\subsection{Input Data}

Input data was artificially generated by an LLM. The prompt took inspirations from

We split the data by 2 tracks. Software development and Data Science. Tools between tracks are differ enough to warrant a split in tracks.

\subsection{Agent Skill}

An agent skill is a reusable prompt. It turns a general purpose AI agent into a specialist.
A skill may be loaded automatically by an agent based on the skill's description, so it's vital for the author to provide a descriptive skill that matches the


An agent skill may be also manually loaded by the user. In Antigravity CLI, a skill may be invoked by

A skill is a markdown file that contains the following yaml front matter metadata:
\begin{itemize}
	\item name: Name of the skill
	\item description: The keywords that agents matches against a user's prompt to determine whether to trigger the skill.
\end{itemize}



\begin{lstlisting}[caption=Course flake generator metadata]
---
name: course-flake-generator
description: Generates a nix flake file based on an instructor's requirements or with a provided project folder with source code and other artifacts. Use when creating, generating, or configuring a Nix flake (flake.nix) for course assignments, lab environments, or software projects from specifications, natural language instructions, or existing codebases. Don't use for non-Nix environments, simple shell scripts, or general documentation generation.
---
\end{lstlisting}

for recurrent behaviors as it improves behavioral consistency.

In the case of generating a flake, creating a skill for

We generated a skill for this purpose, with the assistance of a  \href{https://github.com/mgechev/skills-best-practices}{skill} on best practice for creating agent skill with Gemini 3.7 Flash, reasoning set to high.

\begin{lstlisting}[caption=custom skill filetree structure]
.
├── assets
│   ├── flake-devshell.template.nix
│   └── mcp-servers.template.json
├── references
│   ├── flake-patterns.md
│   ├── inferring-requirements.md
│   └── mcp-nixos.md
├── scripts
│   ├── detect-project-requirements.py
│   ├── query-nixos.py
│   └── validate-flake.py
└── SKILL.md
\end{lstlisting}

According to \cite{lingAgentSkillsDataDriven2026},

With agy, we had to manually refer to the skill via \verb|/course_flake_generator|.

Before the addition of skills, the agent attempted to install the dependencies from the input data imperatively via brew, as the OS I conducted this test is with a MacBook.

This is a limitation with skills

On prompts focusing on data science tracks, those inputs resulted in a lot of fails, despite specifying the custom skill, the agent opted to install dependencies via brew, resulting in no flake.nix file being generated. Skills aren't guaranteed to be deterministic


\subsubsection{Sources}


\subsubsection{Format}

\subsection{Pass@K}

\section{Comparison between Nix and DevContainer hardware resource usage}

This tests answers RQ2.

Due to data centers strong demand for computer memory, DRAM manufacturers prioritize profitability

\chapter{Testing Results}

\section{Gemini}
Perform a group by for tool\_calls to track the most common tool calls throughout the tests

Gemini 3.8 Flash set to Medium reasoning

Analyzing the frequency of the different tool calls, the model used the nix CLI instead of the Nix MCP server

Gemini 3.8 training data is based off of Gemini 3. According to \cite{} with a knowledge cutoff of January 2025
Gemini's training data likely contains information on popular nix packages used for development

\section{Qwen 2.5 Coder 14B}

\section{Security Considerations with flakes}

Abstracting away

